#include <stdio.h>                   //con esto incluyo la libreria standar io, entradas  y salidas .h indica que es una cabecera de C

int main(int argc,char *argv[]){     //Tengo que declarar un punto de entrada de ejecución se tiene que llamar obligatoriamente main
                                     //seguido de char y un array de argumentos
    
    printf("Hola Carlos Jimenez \n");
    
    return 0;                        //como indiqué en la declaración tiene que devolver un int, un número entero
}

